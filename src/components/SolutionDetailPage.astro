---
import BaseLayout from '../layouts/BaseLayout.astro'
import type { SolutionPage } from '../data/solutionPages'

const { solution } = Astro.props as { solution: SolutionPage }

const breadcrumbSchema = {
  '@context': 'https://schema.org',
  '@type': 'BreadcrumbList',
  itemListElement: [
    { '@type': 'ListItem', position: 1, name: 'Home', item: 'https://naurra.ai/' },
    { '@type': 'ListItem', position: 2, name: 'Solutions', item: 'https://naurra.ai/solutions/' },
    { '@type': 'ListItem', position: 3, name: solution.name, item: `https://naurra.ai/solutions/${solution.slug}/` }
  ]
}

const serviceSchema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  serviceType: 'Custom AI Agent Development',
  name: `${solution.name} for Small Business`,
  description: solution.description,
  url: `https://naurra.ai/solutions/${solution.slug}/`,
  image: `https://naurra.ai${solution.image}`,
  mainEntityOfPage: `https://naurra.ai/solutions/${solution.slug}/`,
  provider: {
    '@type': 'ProfessionalService',
    name: 'Naurra.ai',
    url: 'https://naurra.ai/',
    logo: 'https://naurra.ai/logo-transparent.png',
    founder: {
      '@type': 'Person',
      name: 'Athanasios-Ioannis Panagiotakopoulos',
      jobTitle: 'CEO and AI Engineer'
    }
  },
  areaServed: 'Worldwide',
  audience: { '@type': 'BusinessAudience', audienceType: 'Small Business' },
  offers: {
    '@type': 'AggregateOffer',
    priceCurrency: 'EUR',
    lowPrice: solution.lowPrice,
    highPrice: solution.highPrice,
    availability: 'https://schema.org/InStock',
    eligibleQuantity: { '@type': 'QuantitativeValue', value: '1' }
  }
}

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: solution.faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.answer
    }
  }))
}

const gradientText = `background: linear-gradient(135deg, ${solution.theme.primary}, ${solution.theme.secondary}); -webkit-background-clip: text; -webkit-text-fill-color: transparent;`
const primaryPill = `background: ${solution.theme.primarySoft}; border: 1px solid ${solution.theme.primaryBorder}; color: ${solution.theme.primary};`
const secondaryPill = `background: ${solution.theme.secondarySoft}; border: 1px solid ${solution.theme.secondaryBorder}; color: ${solution.theme.secondary};`
const neutralPill = 'background: rgba(255,255,255,0.04); border: 1px solid rgba(255,255,255,0.07); color: rgba(255,255,255,0.62);'
const orbPrimary = `background: ${solution.theme.primarySoft};`
const orbSecondary = `background: ${solution.theme.secondarySoft};`
const sectionBar = `background: linear-gradient(180deg, ${solution.theme.primary}, ${solution.theme.secondary});`
const ctaButton = `background: linear-gradient(135deg, ${solution.theme.primary}, ${solution.theme.secondary}); box-shadow: 0 16px 48px ${solution.theme.primarySoft}; border: 1px solid ${solution.theme.primaryBorder};`
const softPrimaryCard = `background: linear-gradient(135deg, ${solution.theme.primarySoft}, rgba(255,255,255,0.01)); border: 1px solid ${solution.theme.primaryBorder};`
const softSecondaryCard = `background: linear-gradient(135deg, ${solution.theme.secondarySoft}, rgba(255,255,255,0.01)); border: 1px solid ${solution.theme.secondaryBorder};`
---

<BaseLayout
  title={solution.seoTitle}
  description={solution.description}
  keywords={solution.keywords}
  image={solution.image}
  imageAlt={solution.imageAlt}
  url={`https://naurra.ai/solutions/${solution.slug}/`}
  structuredData={[breadcrumbSchema, serviceSchema, faqSchema]}
>
  <div class="min-h-screen bg-[#0a0e1a] text-white overflow-hidden" style="font-family: 'Outfit', -apple-system, BlinkMacSystemFont, sans-serif;">
    <div class="fixed inset-0 pointer-events-none">
      <div class="absolute inset-0 bg-gradient-to-br from-white/[0.02] via-transparent to-transparent"></div>
      <div class="absolute top-0 -right-1/4 w-[800px] h-[800px] rounded-full blur-[120px]" style={orbPrimary}></div>
      <div class="absolute bottom-0 -left-1/4 w-[800px] h-[800px] rounded-full blur-[120px]" style={orbSecondary}></div>
    </div>

    <nav class="relative border-b border-white/10 bg-[#0a0e1a]/95 backdrop-blur-xl sticky top-0 z-40">
      <div class="max-w-7xl mx-auto px-6 lg:px-8 py-5 flex items-center justify-between">
        <a href="/" class="flex items-center gap-3 group">
          <div class="relative w-10 h-10 flex items-center justify-center">
            <div class="absolute inset-0 rounded-full blur-xl transition-all group-hover:opacity-90" style={orbPrimary}></div>
            <img src="/logo-transparent.png" alt="Naurra.ai" class="w-full h-full object-contain relative z-10" />
          </div>
          <span style={`font-family: 'Sora', sans-serif; font-weight: 800; letter-spacing: -0.04em; font-size: 20px; ${gradientText}`}>
            Naurra.ai
          </span>
        </a>
        <div class="flex items-center gap-4 flex-wrap justify-end">
          <a href="/solutions/" class="text-sm text-gray-400 hover:text-white transition-colors font-medium">Solutions</a>
          <a href="/company/" class="text-sm text-gray-400 hover:text-white transition-colors font-medium">Case Studies</a>
          <a href="/blog/" class="text-sm text-gray-400 hover:text-white transition-colors font-medium">Blog</a>
          <a href="/contact/" class="text-sm font-semibold px-5 py-2 rounded-lg transition-all text-white" style={ctaButton}>Book a free call</a>
        </div>
      </div>
    </nav>

    <div class="relative max-w-7xl mx-auto px-6 lg:px-8 pt-8">
      <nav class="text-xs text-gray-500 font-mono uppercase tracking-wider" aria-label="Breadcrumb">
        <a href="/" class="hover:text-white transition-colors">Home</a>
        <span class="mx-2">/</span>
        <a href="/solutions/" class="hover:text-white transition-colors">Solutions</a>
        <span class="mx-2">/</span>
        <span class="text-gray-300">{solution.name}</span>
      </nav>
    </div>

    <div class="relative max-w-7xl mx-auto px-6 lg:px-8 py-12 lg:py-16">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 mb-24 items-center">
        <div>
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full mb-6" style={softPrimaryCard}>
            <span class="w-2 h-2 rounded-full animate-pulse" style={`background: ${solution.theme.primary};`}></span>
            <span class="text-xs font-mono uppercase tracking-widest" style={`color: ${solution.theme.primary};`}>{solution.heroLabel}</span>
          </div>

          <h1 style="font-family: 'Sora', sans-serif; font-weight: 800; letter-spacing: -0.04em;" class="text-5xl lg:text-6xl leading-[0.95] tracking-tight mb-6">
            <span class="text-white">{solution.heroTitleTop}</span><br />
            <span style={gradientText}>{solution.heroTitleBottom}</span>
          </h1>

          <p class="text-xl text-gray-400 leading-relaxed mb-8">
            {solution.heroDescription}
          </p>

          <div class="flex flex-wrap gap-3 mb-8">
            <span class="px-4 py-2 rounded-full text-sm font-mono" style={primaryPill}>{solution.priceRange}</span>
            <span class="px-4 py-2 rounded-full text-sm font-mono" style={secondaryPill}>{solution.timeline}</span>
            <span class="px-4 py-2 rounded-full text-sm font-mono" style={neutralPill}>{solution.runningCost}</span>
          </div>

          <div class="flex flex-col sm:flex-row gap-4">
            <a href="/contact/" class="px-8 py-4 rounded-xl font-semibold text-lg text-white transition-all hover:opacity-95 text-center" style={ctaButton}>
              Book a free consultation
            </a>
            <a href="#how-it-works" class="px-8 py-4 bg-white/5 border border-white/10 rounded-xl text-gray-400 hover:bg-white/10 hover:text-white transition-all font-medium text-center">
              See how it works
            </a>
          </div>
        </div>

        <div class="relative">
          <div class="absolute -inset-4 rounded-3xl blur-2xl" style={`background: linear-gradient(135deg, ${solution.theme.primarySoft}, ${solution.theme.secondarySoft});`}></div>
          <div class="relative bg-gradient-to-br from-gray-800/70 to-gray-900/70 border border-gray-700/50 rounded-3xl p-8">
            <div class="text-xs font-mono uppercase tracking-widest mb-4" style={`color: ${solution.theme.primary};`}>{solution.proofLabel}</div>
            <p style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.03em;" class="text-3xl lg:text-4xl text-white leading-tight mb-6">
              <span style={gradientText}>{solution.proofMetric}</span>
            </p>
            <p class="text-gray-400 leading-relaxed mb-6">
              {solution.proofBody}
            </p>
            <div class="flex items-center justify-between pt-6 border-t border-white/5">
              <div>
                <div class="text-sm text-white font-semibold">{solution.proofSource}</div>
                <div class="text-xs text-gray-500">{solution.proofContext}</div>
              </div>
              <a href="/company/" class="text-sm font-semibold flex items-center gap-1" style={`color: ${solution.theme.primary};`}>
                Read case studies
                <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" /></svg>
              </a>
            </div>
          </div>
        </div>
      </div>

      <div class="max-w-3xl mx-auto mb-24" style="color: rgba(255,255,255,0.74); font-size: 17px; line-height: 1.8;">
        <h2 style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.02em;" class="text-3xl lg:text-4xl text-white mb-6">{solution.problemTitle}</h2>
        {solution.problemParagraphs.map((paragraph) => (
          <p class="mb-4">{paragraph}</p>
        ))}
      </div>

      <div id="how-it-works" class="mb-24">
        <div class="flex items-center gap-3 mb-10">
          <div class="w-1 h-8 rounded-full" style={sectionBar}></div>
          <h2 style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.02em;" class="text-3xl text-white">{solution.capabilitiesTitle}</h2>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
          {solution.capabilities.map((capability, index) => (
            <div class="bg-gradient-to-br from-gray-800/50 to-gray-900/50 border border-gray-700/50 rounded-2xl p-6">
              <div class="text-xs font-mono mb-3" style={`color: ${solution.theme.primary};`}>{String(index + 1).padStart(2, '0')}</div>
              <h3 style="font-family: 'Sora', sans-serif; font-weight: 700;" class="text-lg text-white mb-2 leading-tight">{capability.title}</h3>
              <p class="text-sm text-gray-400 leading-relaxed">{capability.body}</p>
            </div>
          ))}
        </div>
      </div>

      <div class="mb-24">
        <div class="flex items-center gap-3 mb-10">
          <div class="w-1 h-8 rounded-full" style={sectionBar}></div>
          <h2 style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.02em;" class="text-3xl text-white">{solution.stepsTitle}</h2>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-4 gap-5">
          {solution.steps.map((step) => (
            <div class="bg-gradient-to-br from-gray-800/50 to-gray-900/50 border border-gray-700/50 rounded-2xl p-6">
              <div class="text-xs font-mono mb-3" style={`color: ${solution.theme.primary};`}>{step.label}</div>
              <h3 class="text-lg text-white font-semibold mb-2">{step.title}</h3>
              <p class="text-sm text-gray-400 leading-relaxed">{step.body}</p>
            </div>
          ))}
        </div>
      </div>

      <div class="mb-24">
        <div class="flex items-center gap-3 mb-10">
          <div class="w-1 h-8 rounded-full" style={sectionBar}></div>
          <h2 style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.02em;" class="text-3xl text-white">{solution.compareTitle}</h2>
        </div>
        <div class="overflow-x-auto rounded-2xl border border-gray-700/50">
          <table class="w-full text-left">
            <thead class="bg-white/5 text-xs font-mono uppercase tracking-wider text-gray-400">
              <tr>
                <th class="px-6 py-4 font-semibold"></th>
                <th class="px-6 py-4 font-semibold">{solution.compareHeaders.baseline}</th>
                <th class="px-6 py-4 font-semibold">{solution.compareHeaders.middle}</th>
                <th class="px-6 py-4 font-semibold" style={`color: ${solution.theme.primary};`}>{solution.compareHeaders.custom}</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-700/50 text-sm">
              {solution.compareRows.map((row) => (
                <tr class="hover:bg-white/[0.02]">
                  <td class="px-6 py-4 text-gray-300 font-medium">{row.label}</td>
                  <td class="px-6 py-4 text-gray-500">{row.baseline}</td>
                  <td class="px-6 py-4 text-gray-500">{row.middle}</td>
                  <td class="px-6 py-4" style={`color: ${solution.theme.primary};`}>{row.custom}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
        <p class="text-sm text-gray-500 mt-4">
          For the broader decision framework, read our
          <a href="/blog/custom-ai-agents-vs-off-the-shelf-ai-which-one-does-your-business-need/" class="font-semibold ml-1" style={`color: ${solution.theme.primary};`}>custom vs off-the-shelf AI guide</a>.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-24">
        <div class="rounded-2xl p-7" style={softPrimaryCard}>
          <h3 style="font-family: 'Sora', sans-serif; font-weight: 700;" class="text-xl text-white mb-4 flex items-center gap-2">
            <svg class="w-5 h-5" style={`color: ${solution.theme.primary};`} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" /></svg>
            When the math works
          </h3>
          <ul class="space-y-2.5 text-sm text-gray-300 leading-relaxed">
            {solution.whenWorks.map((item) => (
              <li class="flex gap-2"><span class="mt-0.5" style={`color: ${solution.theme.primary};`}>+</span><span>{item}</span></li>
            ))}
          </ul>
        </div>
        <div class="rounded-2xl p-7" style={softSecondaryCard}>
          <h3 style="font-family: 'Sora', sans-serif; font-weight: 700;" class="text-xl text-white mb-4 flex items-center gap-2">
            <svg class="w-5 h-5" style={`color: ${solution.theme.secondary};`} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
            When it does not
          </h3>
          <ul class="space-y-2.5 text-sm text-gray-300 leading-relaxed">
            {solution.whenNot.map((item) => (
              <li class="flex gap-2"><span class="mt-0.5" style={`color: ${solution.theme.secondary};`}>-</span><span>{item}</span></li>
            ))}
          </ul>
        </div>
      </div>

      <div class="mb-24">
        <div class="flex items-center gap-3 mb-10">
          <div class="w-1 h-8 rounded-full" style={sectionBar}></div>
          <h2 style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.02em;" class="text-3xl text-white">Frequently asked questions</h2>
        </div>
        <div class="space-y-4">
          {solution.faqs.map((faq) => (
            <details class="group bg-gradient-to-br from-gray-800/40 to-gray-900/40 border border-gray-700/50 rounded-2xl overflow-hidden">
              <summary class="cursor-pointer list-none px-6 py-5 flex items-center justify-between hover:bg-white/[0.02]">
                <span class="text-white font-semibold text-base pr-4">{faq.question}</span>
                <svg class="w-5 h-5 flex-shrink-0 transition-transform group-open:rotate-180" style={`color: ${solution.theme.primary};`} fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
              </summary>
              <div class="px-6 pb-5 text-sm text-gray-400 leading-relaxed">{faq.answer}</div>
            </details>
          ))}
        </div>
      </div>

      <div class="mb-24">
        <div class="flex items-center gap-3 mb-8">
          <div class="w-1 h-8 rounded-full" style={sectionBar}></div>
          <h2 style="font-family: 'Sora', sans-serif; font-weight: 700; letter-spacing: -0.02em;" class="text-2xl text-white">Related guides</h2>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
          {solution.relatedGuides.map((guide) => (
            <a href={guide.href} class="group block bg-gradient-to-br from-gray-800/50 to-gray-900/50 border border-gray-700/50 rounded-2xl p-6 hover:border-white/20 transition-all">
              <span class="text-xs font-mono uppercase tracking-wider" style={`color: ${solution.theme.primary};`}>{guide.label}</span>
              <h3 class="text-white font-semibold mt-2 group-hover:text-white transition-colors">{guide.title}</h3>
              <p class="text-sm text-gray-500 mt-2">{guide.body}</p>
            </a>
          ))}
        </div>
      </div>

      <div class="bg-gradient-to-br from-gray-800/60 to-gray-900/60 border border-gray-700/50 rounded-3xl p-12 lg:p-16 text-center">
        <h2 style="font-family: 'Sora', sans-serif; font-weight: 800; letter-spacing: -0.04em;" class="text-3xl lg:text-4xl text-white mb-4">
          {solution.ctaTitle}
        </h2>
        <p class="text-lg text-gray-400 max-w-xl mx-auto mb-8">
          {solution.ctaBody}
        </p>
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
          <a href="/contact/" class="px-8 py-4 rounded-xl font-semibold text-lg text-white transition-all hover:opacity-95" style={ctaButton}>
            Book a free consultation
          </a>
          <a href="/company/" class="px-8 py-4 bg-white/5 border border-white/10 rounded-xl text-gray-400 hover:bg-white/10 hover:text-white transition-all font-medium">
            See more case studies
          </a>
        </div>
      </div>
    </div>

    <footer class="relative border-t border-gray-700/50 bg-[#0a0e1a] mt-20">
      <div class="max-w-7xl mx-auto px-6 lg:px-8 py-8 flex flex-col md:flex-row items-center justify-between gap-6">
        <p class="text-gray-500 text-sm">&copy; 2026 Naurra.ai. All rights reserved.</p>
        <div class="flex items-center gap-6">
          <a href="/privacy/" class="text-gray-500 hover:text-gray-300 text-sm transition-colors">Privacy</a>
          <a href="/terms/" class="text-gray-500 hover:text-gray-300 text-sm transition-colors">Terms</a>
          <a href="/blog/" class="text-gray-500 hover:text-gray-300 text-sm transition-colors">Blog</a>
          <a href="/contact/" class="text-gray-500 hover:text-gray-300 text-sm transition-colors">Contact</a>
        </div>
      </div>
    </footer>
  </div>
</BaseLayout>
